---
description: Use when you need token-efficient distillates that preserve all information for downstream LLM consumption.
agent: general
---

Use the bmad-distillator skill to perform the task with arguments: $ARGUMENTS
